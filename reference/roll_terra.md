# In-Memory Rolling Time Series Averaging (Internal)

In-Memory Rolling Time Series Averaging (Internal)

## Usage

``` r
roll_terra(
  x,
  window_size,
  freq,
  step,
  fun,
  output_names_list,
  output_dir,
  gdal_opt,
  overwrite,
  verbose
)
```
