# In-Memory Time Series Aggregation (Internal)

In-Memory Time Series Aggregation (Internal)

## Usage

``` r
aggregate_terra(
  x,
  index,
  output_names,
  output_dir,
  gdal_opt,
  overwrite,
  verbose,
  aggregation_type = "mean"
)
```
