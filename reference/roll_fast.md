# Tiled, Out-of-Core Rolling Time Series Averaging (Internal)

Tiled, Out-of-Core Rolling Time Series Averaging (Internal)

## Usage

``` r
roll_fast(
  paths,
  window_size,
  freq,
  step,
  fun,
  output_names_list,
  user_region,
  tile_degrees,
  output_dir,
  verbose
)
```
