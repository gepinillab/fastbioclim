# Tiled, Out-of-Core Time Series Aggregation (Internal)

Tiled, Out-of-Core Time Series Aggregation (Internal)

## Usage

``` r
aggregate_fast(
  paths,
  index,
  output_names,
  user_region,
  tile_degrees,
  output_dir,
  verbose,
  aggregation_type = "mean"
)
```
