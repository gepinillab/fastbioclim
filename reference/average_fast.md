# Tiled, Out-of-Core Time Series Averaging (Internal)

Tiled, Out-of-Core Time Series Averaging (Internal)

## Usage

``` r
average_fast(
  paths,
  index,
  output_names,
  user_region,
  tile_degrees,
  output_dir,
  verbose
)
```
